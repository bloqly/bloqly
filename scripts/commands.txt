# install postgres

macos:
createuser -s postgres
createdb bloqly_test


BLOQLY_HOME=../../demo/main ./release/mac/bq.sh
BLOQLY_HOME=../../demo/rhea ./release/mac/bq.sh
BLOQLY_HOME=../../demo/loge ./release/mac/bq.sh
BLOQLY_HOME=../../demo/ymir ./release/mac/bq.sh

# Linux machine:

sudo apt-get update
sudo apt-get install postgresql postgresql-contrib

sudo -su postgres

createuser bloqly_user
createdb bloqly

psql -d bloqly -c "alter user bloqly_user with password 'password';"

LOG_PATH=./logs BLOQLY_CONFIG=/home/slava/bloqly/bloqly.yaml ./dist/bq.sh